<%- include('./partials/head'); -%>

<h1><%= title %></h1>
<ul>
  <% messages.forEach(message => { %>
  <li>
    <strong><%= message.user %></strong>: <%= message.text %> (added <%=
    message.added.toLocaleString() %>)
  </li>
  <% }); %>
</ul>
<a href="/new">Add a new message</a>
